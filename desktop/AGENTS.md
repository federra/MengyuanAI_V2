# Desktop releases

- 用户要求每次发布更新都递增版本号，方便区分（2026-09-14）。每次交付新桌面更新前，更新 desktop/package.json 的 version，至少增加 patch。
- package.json 是桌面版本的唯一来源；打包、窗口标题、侧栏及“关于”读取该版本，不另写固定版本号。
- 新版本使用独立版本目录，完成构建和验证后更新桌面快捷方式。保留旧版本及用户数据，不替换正在运行的程序。
- 交付时说明实际发布版本、功能入口及是否需要保存项目后重启。不要把仅源码修改说成运行中的软件已更新。
