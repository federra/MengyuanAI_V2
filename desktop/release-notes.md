# 0.1.76

- 分镜生成、导入和编辑后应用统一校验；已知外部Agent格式直接转换，保留台词、提示词、资产及音色；其他格式最多调用一次文本模型转换后复核。
- 修复完整模型响应缺少结束标记时被误判失败；区分输出截断、空响应等原因。
- 角色默认启用三视图及“角色三视图生图”Skill，仍可自行取消或更换。
- 角色、场景、道具管理固定表头；修复清除图片后历史结果回显，分开“预览”和“应用/替换”按钮。
- 图片目录保留系统原生选图窗口，定位当前账号和项目的图片目录，支持重新选择历史图片。

本次发布Windows x64，支持软件内更新。Mac更新渠道维持0.1.75。
