# 左侧导航分组调整

2026-09-24。用户已批准效果图，并补充要求保持页面与组件原有颜色，仅调整布局、字体及折叠入口。

## 实现

- 创作：项目中心、创作中心、任务中心；知识：资产中心、skill中心；设置：模型设置、系统设置；经营：收益中心、渠道代理、管理后台。
- 类别12px、页面15px，页面文字相对类别缩进16px。展开时仅显示页面文字；收起时显示原有图标与名称提示。导航独立滚动，短窗口仍可访问末尾入口。
- 侧栏顶部新增收起/展开图标按钮，使用原Sidebar状态；保留主内容区开关以支持窄屏。按钮提供中文名称、展开状态和原生键盘操作。
- 创作中心相关跳转与状态判断同步更名；管理后台页面标题同步更名且保留超管校验。系统设置继续打开原弹窗。侧栏移除剪辑输出独立入口，既有成品导出及资产页导出路径保留，处于剪辑输出时高亮创作中心。
- 未修改颜色值、主题变量、鉴权接口、用户数据、模型请求或导出实现。

## 本轮验证与边界

- `npx tsc --noEmit`、四个修改TSX文件定向`oxlint`、`npm run build`通过。构建保留框架弃用和大chunk提示，没有构建错误。
- 浏览器隔离页面直接提取当前导航JSX及分组配置，加载实际Sidebar组件和应用全部样式。验证四组十入口、普通会员隐藏管理后台、页面切换回调、系统设置回调、鼠标及回车收起/展开、深浅主题和1280×768窗口下滚动可达。
- 隔离检查仅使用内存模拟账号与回调，没有连接业务数据或真实模型；不等同于正式Electron整链路验收。临时文件位于忽略目录`work/navigation-ui-2026-09-24/`，检查服务已停止。
- 本轮仅交付源码；未升版本、重新打包桌面、提交推送或发布。现有桌面进程保持原版本。

## 同日：Mac本机0.1.77交付

用户随后明确要求启动修改后的桌面端。按桌面约定将`desktop/package.json`升至0.1.77，更新本版说明，重新执行构建及`desktop/package-mac.mjs`。

- 本机入口：`desktop/release/v0.1.77/mac/启动桌面版.command`。桌面新增同名版本快捷入口。使用现有工程依赖，不是独立分发安装包。
- 隔离目录：`desktop/test-results/navigation-0.1.77-smoke/`。实际Electron启动与关闭通过；登录页正常、未认证项目接口401、Node访问undefined。9个客户端JS与最新dist逐字节一致，并确认包含新导航。
- 随后打开正式本地数据窗口，原生窗口检查确认侧栏版本0.1.77、四组十个入口及收起按钮均已显示，当前项目显示已保存。未编辑项目或提交模型任务。
- 保留旧版本及用户数据。未提交推送、制作Windows包或发布在线更新，公开渠道不变。
