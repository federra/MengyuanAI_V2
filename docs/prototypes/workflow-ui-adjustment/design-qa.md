# 低保真原型验证记录

2026-09-23；验证对象为本目录独立HTML原型，浏览器为Codex内置浏览器。不是正式系统测试，不包含真实模型、豆包、媒体播放、文件导出或Windows实机验收。

## 已走查

- 从idea路径重置演示；把X改为2，生成2个候选并进入预览。
- 采用故事A、手工修改正文、预览候选B、返回当前正文：手改内容保留；下一步剧本预览显示使用当前稿。
- 采用剧本、预览并应用分镜：产生6个待制作镜头。
- 展开镜头1修改画面描述，折叠后摘要显示新内容。
- 切换要求图片的示意模型、关闭参考图，提交被阻止并显示补齐/换模型提示；切回文生视频，无图可模拟提交。
- 模拟视频结果就绪→预览→采用到原镜头；任务显示“当前使用”，成品导出画面数同步增加。
- 导出缺少画面时显示具体镜头号；返回缺失镜头能展开对应镜头。确认模拟剪映草稿导出后显示“演示完成”，没有生成真实文件。
- 刷新保留原型草稿；导入示例故事直接进入可编辑当前正文。
- 清除资产当前图，弹窗立即显示“尚无图片”；模拟场景图片生成、预览、采用显示当前使用。
- 记录审核意见→关闭→重新打开，文字保留；临时测试意见已清空。
- 导出选择器包含剪映草稿、完整剪辑包、字幕SRT、项目JSON、分镜JSON五种；Escape关闭后焦点回到“导出选项”。

## 布局与技术检查

- 1440×900截图检查：流程总览及分镜总览结构清晰；分镜前四条摘要底边约542、646、750、854px，均完整位于首屏内。
- 1366×768截图检查：成品导出播放器、镜头条与问题清单同屏，导出入口可见；页面无整体横向溢出。
- 浏览器错误日志查询未返回error。
- `node --check docs/prototypes/workflow-ui-adjustment/demo.js`通过。
- 文件依赖均本地；不调用fetch、模型API或正式桌面IPC。

## 限制

所有模拟产物为线框占位；生成采用固定示例，不是真实AI创作。代码包含批量、表格、失败示例、原生文件选择等入口，但本记录不把未逐项实测的交互宣称全面通过。原型没有模拟完整业务权限、生产恢复或所有平台。正式实现仍需原方案所列回归与复用计时。
